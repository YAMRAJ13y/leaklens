# Copy to .env (gitignored) only for the optional Claude weekly intel brief (--llm).
# The dashboard and the --live ransomware.live feed need NONE of these (both keyless).

ANTHROPIC_API_KEY=
LEAKLENS_MODEL=claude-haiku-4-5
